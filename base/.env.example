# Copy to base/.env (never commit .env) only if you use the cloud bridge
# with a hosted VLM. Empty/missing = bridge runs local-only, hints disabled.
# The compose file reads this via ${OPENAI_API_KEY:-} — nothing secret lives here.
OPENAI_API_KEY=
